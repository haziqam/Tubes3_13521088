import { foundQuestion } from "./interface";
import { getQnA } from "./question";

function shiftTable(toMatch: String): number[]{
    let start: number = 0;
    let i: number = 0;
    const stringLength: number = toMatch.length;
    let shiftTable: number[] = [];

    for(let i = 0; i < stringLength; i++){
        
    }

    return shiftTable;
}
  
